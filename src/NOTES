Current complicatedish idea, noted down to not lose track: Implement graph manager that reads dropdown and from that reads graph type. 
Has array of nodes (implemented from json?) and for that array of nodes has info on that node to properly generate and position it since html elem note parameter stuff is a pain atm. 
Make it also handle mouseevents, maybe? MDPApp has central mouseevent setter but passes on to GM subfunc with neccesary data?


TODOS: 
-> make node generate from position in params
-> make nodes identifiable via ID in an actionable way
- implement zoom
- make results one result per paper instead of one per result in jsons
- make graph element clickable popup info view
- generate graph from json
- load in node data from results 
- proper nodes
- connecting arrows
- make actual complexity result graph 
- clean up project again. yes, its commented, but this gmdpapp-graphgen-genericnode thing will still make it a bit unorganized and so putting everything back in its proper place might help

Far-off todos:
Implement subgraphs (i.e. POMDP reachability subgraph for just differing approaches to it)
Implement By-Problem view instead of By-Type view
Settings
More options beneath dropdown




/node-category-values:
TODO make complexity class only have class names and suffix have suffixes
"complexityClasses": [
"NC",
"P",
"NP",
"PSPACE"
],
"complexitySuffix": [
"complete",
"Complete",
"hard",
"Hard",
],