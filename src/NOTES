TODOS: 
- Make child nodes work: Child nodes filter all parent node results down. 
-- maybe make child nodes automatically reorder based on variable? (generic node -> policy comp-> horizon-> determinism becomes generic->policy comp-> det->horizon or filter out: generic-> policy comp-> horizon)
- make zoom keep consistent position (again)
- proper nodes
- connecting arrows
- make actual complexity result graph 
- comment or rename code a bit more, its clear right now and shouldnt be complex however still, it helps

Far-off todos:
Implement subgraphs (i.e. POMDP reachability subgraph for just differing approaches to it)
Implement By-Problem view instead of By-Type view
Settings
More options beneath dropdown (as in, filtering)
make maxtitlelength for node title length limitation dynamic
remove "any" type for mdp results and specify actual type

Graph exporter: using a set graph as a base, move around/add nodes as you see fit (buttonevents disabled), hit save button to bring up prompt to name it to then export to other folder, from which it can be moved manually into graph configs to use in future

