TODOS: 

add upload button to grapheditor

help button for advice



        //TODO CHECK FOR DUPLICATE GRAPH TYPES!

add disclaimer that in case of doubt original paper is correct and that this is ment to be an easy overview of the literature that directs you toward the complexity result, not a standin library that directly provides it 
[it will get more accurate over time anyhow, first results just might be rough since i was just starting out then]

add muller objectives to graph for pomdp

rework config to have seperation between general, specific and special edge cases (current is general&specific, seperately special edge cases)

make toggle buttons undo toggle and effects when pressed again

! state somewhere that top bar isnt automatic and extending it requires manual color adjustments else not sensible (default blue)
! make color list automatic and load from config. allow manual adjustment in options menu and saving it to node-category-values.json

Implement By-Problem view instead of By-Type view
Settings
More options beneath dropdown (as in, filtering)

fix connections bug (with 3 depth children esp) (auto tethers to parent if clicking a child node. parent nodes unaffected)
-> maybe leave current unintentional functionality as "link to parent"?
fix creation offset


remember, global filters should affect how nodes are displayed (i.e if side nodes are filtered to only view nonstationary, displayed complexity should update to only view avg of nonstationary)
make global filters have a filter to show "special" cases 


automate config indexing using github actions

make errors a red popup instead of console writes?

maybe add manual "load local configs (from x folder)" button?

allow options menu to edit color 

include stochastic game node in RMDP graph

validator that checks if all result configs have valid assignments that can be read in based on node category values